{
  "rules": {
    "no-todo": true,
    "sentence-length": {
      "max": 200
    },
    "terminology": {
      "terms": {
        "Github": "GitHub",
        "API Endpoint": "API endpoint",
        "Kubernetes Cluster": "Kubernetes cluster"
      }
    }
  }
}